# v0.1 Sprint 0 — Skeleton Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立一个能启动、能播本地音频文件、能读写 SQLite、能通过 Anthropic 与 DeepSeek 两个 provider 完成 LLM 调用、并能在 keychain 里安全存放 API keys 的 Tauri + React 空壳 app。**Sprint 0 完成时，app 里没有音乐 agent 的任何逻辑，但所有基础设施 primitives 都在位。**

**Architecture:** Tauri 2 桌面壳（Rust 后端） + React 18 前端（TypeScript）+ SQLite（`tauri-plugin-sql`）+ rodio 音频渲染 + `keyring-rs` 系统 keychain。前端通过 Tauri IPC 调用 Rust 命令；LLM 调用是从前端直接 HTTPS 到各 provider。所有代码放在 `app/` 下。

**Tech Stack:**
- Tauri 2.x (Rust)
- React 18.x + TypeScript 5.x
- Vite (via `create-tauri-app` react-ts template)
- pnpm 8+ 作为包管理器
- Vitest + @testing-library/react 作为 TS 测试
- `cargo test` 作为 Rust 测试
- `rodio` 0.19 for audio
- `tauri-plugin-sql` (SQLite bundle mode)
- `keyring` (Rust crate) for secret storage

## Global Constraints

- **Node**: 20 LTS+
- **Rust**: 1.75+
- **pnpm**: 8+
- **Tauri**: 2.x（不要用 Tauri 1.x）
- **Package manager**: pnpm 强制（不要 npm/yarn 混用；`packageManager` 字段锁定在 pnpm）
- **No Python 依赖**（spec 明确无 Python sidecar）
- **All secrets via OS keychain**（`keyring-rs`）——不要写到 config 文件或 .env
- **App bundle identifier**: `com.daoyu.lyra`
- **Primary target OS**: macOS（开发环境；跨平台后续处理）
- **Commit style**: 参考已有 commit `feat(scope): message` / `docs: message`；本 sprint 全部 `feat(lyra):` 前缀
- **Test 命名**: TypeScript 用 `*.test.ts` / `*.test.tsx`；Rust 用 `#[cfg(test)] mod tests` 或 `tests/` 目录
- **无 Python 与无 shell sidecar 二次强调**——所有能力要么在 Rust 里，要么在 TS 里
- **绝不 hardcode API key**——所有 provider 必须从 `keyring` 拉密钥

---

## File Structure

After Sprint 0 the tree should look like:

```
音乐播放器/
  docs/
    superpowers/
      specs/2026-07-06-music-player-design.md
      plans/2026-07-06-v0.1-sprint-0-skeleton.md
  app/
    package.json
    pnpm-lock.yaml
    vite.config.ts
    vitest.config.ts
    tsconfig.json
    index.html
    src/
      main.tsx
      App.tsx
      App.test.tsx
      types/
        dialogue.ts          # DialogueTurn 及相关类型
        soul.ts              # SoulState 类型
        song.ts              # Song / Track / LibraryTrack 类型
        provider.ts          # ModelProvider 接口和 Message 类型
        index.ts             # barrel re-export
      audio/
        player.ts            # TS wrapper for Rust audio commands
        player.test.ts
      db/
        client.ts            # tauri-plugin-sql 客户端封装
        client.test.ts
      providers/
        registry.ts          # Provider registry
        registry.test.ts
        anthropic.ts         # Anthropic adapter
        anthropic.test.ts
        deepseek.ts          # DeepSeek adapter
        deepseek.test.ts
      settings/
        secrets.ts           # TS wrapper for keyring commands
        secrets.test.ts
        Settings.tsx         # Settings 模态框
        Settings.test.tsx
    src-tauri/
      Cargo.toml
      tauri.conf.json
      build.rs
      capabilities/default.json
      src/
        main.rs
        lib.rs               # setup, plugin registration
        audio.rs             # rodio 播放
        secrets.rs           # keyring 封装
      migrations/
        001_initial.sql      # 10 张表初始化 SQL
      tests/
        audio_test.rs
        secrets_test.rs
```

**责任划分**：每个文件一个焦点。`audio/` 只管音频；`db/` 只管 SQLite；`providers/` 只管 LLM；`settings/` 只管配置。这样 Sprint 1 起可以按功能扩展而不用重构文件夹。

---

## Task Dependency Graph

```
Task 1 (bootstrap)
  ├─→ Task 2 (Rust audio)
  │     └─→ Task 3 (TS audio wrapper)
  ├─→ Task 4 (SQLite migrations)
  │     └─→ Task 5 (TS core types)
  ├─→ Task 6 (Provider registry)
  │     ├─→ Task 7 (Anthropic adapter)
  │     └─→ Task 8 (DeepSeek adapter)
  ├─→ Task 9 (Keyring secrets)
  │     └─→ Task 10 (Settings modal)
  └─→ Task 11 (End-to-end smoke)
```

Tasks 2-10 可以在 Task 1 完成后按依赖并行。Task 11 是最后的 acceptance smoke。

---

## Task 1: Bootstrap Tauri 2 + React + TypeScript Scaffold

**Files:**
- Create: `app/` 整个目录（通过 `create-tauri-app`）
- Modify: `app/package.json`（加 vitest deps + scripts）
- Create: `app/vitest.config.ts`
- Create: `app/src/App.test.tsx`

**Interfaces Produced:**
- `pnpm dev` 启动 Tauri app，窗口显示默认 React 界面
- `pnpm test` 跑 Vitest 全绿
- `pnpm build` 能生成 macOS `.app` bundle
- `cd src-tauri && cargo test` 全绿（默认没有 test，通过即可）

**Interfaces Consumed:** 无（这是根任务）

- [ ] **Step 1.1: 检查工具链**

Run:
```bash
node --version    # 需要 >= v20
rustc --version   # 需要 >= 1.75
pnpm --version    # 需要 >= 8
```

Expected: 三个都返回符合要求的版本号。任何一个不满足，先装/升级。

- [ ] **Step 1.2: 用 create-tauri-app 生成 scaffold**

Run:
```bash
cd 音乐播放器
pnpm create tauri-app@latest app --template react-ts --manager pnpm --identifier com.daoyu.lyra
```

如果 CLI 交互还问其它问题：
- app name: `lyra`
- window title: `Lyra`

Expected: 目录 `app/` 被创建，含 `src/`、`src-tauri/`、`package.json`、`vite.config.ts`、`tsconfig.json`。

- [ ] **Step 1.3: 安装依赖**

Run:
```bash
cd app
pnpm install
```

Expected: `node_modules/` 生成，`pnpm-lock.yaml` 生成。无错误。

- [ ] **Step 1.4: 手动 verify dev server 能启动窗口**

Run:
```bash
cd app
pnpm dev
```

Expected: 一个原生窗口弹出，显示默认 Tauri React 页面（有 Tauri 和 React 的 logo 以及 "Click on the Tauri, Vite, and React logos to learn more" 文本）。**手动关闭窗口**（不要 Ctrl-C，让它自己退出——Tauri 会同时结束 dev server）。

- [ ] **Step 1.5: 加 Vitest + testing-library 依赖**

Run:
```bash
cd app
pnpm add -D vitest@^1 @vitest/ui@^1 jsdom@^24 @testing-library/react@^15 @testing-library/jest-dom@^6
```

Expected: 无错误，`package.json` 的 `devDependencies` 里新增这几个。

- [ ] **Step 1.6: 创建 `vitest.config.ts`**

Create file `app/vitest.config.ts`:

```typescript
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./src/test-setup.ts"],
  },
});
```

- [ ] **Step 1.7: 创建 `src/test-setup.ts`**

Create file `app/src/test-setup.ts`:

```typescript
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 1.8: 更新 `package.json` scripts**

Modify `app/package.json`, 找到 `"scripts"` 块，替换成：

```json
"scripts": {
  "dev": "vite",
  "build": "tsc && vite build",
  "preview": "vite preview",
  "tauri": "tauri",
  "test": "vitest run",
  "test:watch": "vitest",
  "test:ui": "vitest --ui",
  "typecheck": "tsc --noEmit"
}
```

同时在 `package.json` 顶层加：
```json
"packageManager": "pnpm@9.0.0"
```
（版本号用实际的 `pnpm --version` 输出）

- [ ] **Step 1.9: 写第一个 failing 测试**

Create file `app/src/App.test.tsx`:

```typescript
import { render, screen } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import App from "./App";

describe("App", () => {
  it("renders the Lyra title", () => {
    render(<App />);
    expect(screen.getByText(/lyra/i)).toBeInTheDocument();
  });
});
```

- [ ] **Step 1.10: 运行测试 → 应该 FAIL**

Run:
```bash
cd app
pnpm test
```

Expected: FAIL，报错说找不到 "lyra" 字样（默认 template 里没有）。

- [ ] **Step 1.11: 修改 `src/App.tsx` 让测试过**

Replace `app/src/App.tsx` 内容为：

```tsx
import "./App.css";

function App() {
  return (
    <main className="container">
      <h1>Lyra</h1>
      <p>Sprint 0 skeleton — nothing here yet.</p>
    </main>
  );
}

export default App;
```

- [ ] **Step 1.12: 运行测试 → 应该 PASS**

Run: `pnpm test`
Expected: PASS。

- [ ] **Step 1.13: Rust 侧 baseline test**

Run:
```bash
cd app/src-tauri
cargo test
```

Expected: 0 tests, 0 failures（默认 template 无测试）。命令返回成功即可。

- [ ] **Step 1.14: 手动 verify 修改后的窗口**

Run:
```bash
cd app
pnpm dev
```

Expected: 窗口显示 "Lyra" + "Sprint 0 skeleton..."。关闭窗口。

- [ ] **Step 1.15: Commit**

Run:
```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app"
git commit -m "$(cat <<'EOF'
feat(lyra): bootstrap Tauri 2 + React + TS scaffold (Sprint 0 T1)

- create-tauri-app react-ts + pnpm
- vitest + testing-library setup
- App smoke test passing

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Rust Audio Playback (rodio)

**Files:**
- Create: `app/src-tauri/src/audio.rs`
- Modify: `app/src-tauri/Cargo.toml` (add `rodio`, `thiserror`)
- Modify: `app/src-tauri/src/lib.rs` (register `audio_*` commands)
- Create: `app/src-tauri/tests/audio_test.rs`
- Create: `app/src-tauri/tests/fixtures/silence_1s.wav`（1 秒静音文件用于测试）

**Interfaces Produced:**

Rust module:
```rust
// audio.rs
#[derive(Debug, thiserror::Error)]
pub enum AudioError {
    #[error("io: {0}")]
    Io(#[from] std::io::Error),
    #[error("decode: {0}")]
    Decode(String),
    #[error("stream: {0}")]
    Stream(String),
    #[error("not initialized")]
    NotInitialized,
}

pub struct AudioPlayer { /* opaque */ }

impl AudioPlayer {
    pub fn new() -> Result<Self, AudioError>;
    pub fn play_file(&self, path: &std::path::Path) -> Result<(), AudioError>;
    pub fn stop(&self);
    pub fn is_playing(&self) -> bool;
}
```

Tauri commands:
```rust
#[tauri::command] async fn audio_play(state: State<'_, AppState>, path: String) -> Result<(), String>;
#[tauri::command] async fn audio_stop(state: State<'_, AppState>) -> Result<(), String>;
#[tauri::command] async fn audio_is_playing(state: State<'_, AppState>) -> Result<bool, String>;
```

**Interfaces Consumed:** 无

- [ ] **Step 2.1: 添加 dependencies**

Modify `app/src-tauri/Cargo.toml`, 在 `[dependencies]` 块下加：

```toml
rodio = { version = "0.19", default-features = false, features = ["symphonia-all"] }
thiserror = "1"
```

Run `cd app/src-tauri && cargo check` — 应该编译通过。

- [ ] **Step 2.2: 创建测试 fixture（1 秒静音 WAV）**

Run:
```bash
cd app/src-tauri
mkdir -p tests/fixtures
# 用系统自带的 afplay/sox 或 ffmpeg 生成静音；此处用 ffmpeg：
ffmpeg -f lavfi -i "anullsrc=r=44100:cl=mono" -t 1 -acodec pcm_s16le tests/fixtures/silence_1s.wav
ls -la tests/fixtures/silence_1s.wav
```

Expected: 文件生成，大小约 88 KB。如无 ffmpeg，安装 `brew install ffmpeg` 或用 Python 生成一个静音 wav。

- [ ] **Step 2.3: 写 failing 测试**

Create `app/src-tauri/tests/audio_test.rs`:

```rust
use lyra_lib::audio::AudioPlayer;
use std::path::PathBuf;
use std::thread::sleep;
use std::time::Duration;

fn fixture(name: &str) -> PathBuf {
    let mut p = PathBuf::from(env!("CARGO_MANIFEST_DIR"));
    p.push("tests/fixtures");
    p.push(name);
    p
}

#[test]
fn create_player_ok() {
    let _p = AudioPlayer::new().expect("new should succeed");
}

#[test]
fn play_file_starts_playback() {
    let p = AudioPlayer::new().unwrap();
    p.play_file(&fixture("silence_1s.wav")).expect("play should succeed");
    assert!(p.is_playing());
}

#[test]
fn stop_ends_playback() {
    let p = AudioPlayer::new().unwrap();
    p.play_file(&fixture("silence_1s.wav")).unwrap();
    p.stop();
    // small wait for internal cleanup
    sleep(Duration::from_millis(50));
    assert!(!p.is_playing());
}

#[test]
fn play_missing_file_returns_error() {
    let p = AudioPlayer::new().unwrap();
    let err = p.play_file(&PathBuf::from("/nonexistent.wav"));
    assert!(err.is_err());
}
```

- [ ] **Step 2.4: 定 crate lib 名以便 tests/ 能引用 `lyra_lib`**

Modify `app/src-tauri/Cargo.toml`, 在 `[package]` 之后加或改：

```toml
[lib]
name = "lyra_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
```

(如果 `[lib]` 已存在，只需保证 `name = "lyra_lib"` 且 `crate-type` 包含 `rlib`。)

- [ ] **Step 2.5: 运行测试 → 应该 FAIL**

Run:
```bash
cd app/src-tauri
cargo test
```

Expected: 编译错误 `unresolved import: audio` 或类似——因为 `audio.rs` 还没写。

- [ ] **Step 2.6: 实现 `audio.rs`**

Create `app/src-tauri/src/audio.rs`:

```rust
use rodio::{Decoder, OutputStream, OutputStreamHandle, Sink};
use std::fs::File;
use std::io::BufReader;
use std::path::Path;
use std::sync::{Arc, Mutex};

#[derive(Debug, thiserror::Error)]
pub enum AudioError {
    #[error("io: {0}")]
    Io(#[from] std::io::Error),
    #[error("decode: {0}")]
    Decode(String),
    #[error("stream: {0}")]
    Stream(String),
    #[error("not initialized")]
    NotInitialized,
}

pub struct AudioPlayer {
    _stream: OutputStream,
    handle: OutputStreamHandle,
    sink: Arc<Mutex<Option<Sink>>>,
}

impl AudioPlayer {
    pub fn new() -> Result<Self, AudioError> {
        let (stream, handle) = OutputStream::try_default()
            .map_err(|e| AudioError::Stream(e.to_string()))?;
        Ok(Self {
            _stream: stream,
            handle,
            sink: Arc::new(Mutex::new(None)),
        })
    }

    pub fn play_file(&self, path: &Path) -> Result<(), AudioError> {
        let file = File::open(path)?;
        let reader = BufReader::new(file);
        let decoder = Decoder::new(reader).map_err(|e| AudioError::Decode(e.to_string()))?;

        let new_sink = Sink::try_new(&self.handle)
            .map_err(|e| AudioError::Stream(e.to_string()))?;
        new_sink.append(decoder);

        let mut guard = self.sink.lock().unwrap();
        *guard = Some(new_sink);
        Ok(())
    }

    pub fn stop(&self) {
        let mut guard = self.sink.lock().unwrap();
        if let Some(sink) = guard.take() {
            sink.stop();
        }
    }

    pub fn is_playing(&self) -> bool {
        let guard = self.sink.lock().unwrap();
        guard.as_ref().map_or(false, |s| !s.empty() && !s.is_paused())
    }
}
```

- [ ] **Step 2.7: 在 `lib.rs` 导出 audio 模块**

Modify `app/src-tauri/src/lib.rs`, 在文件顶部（在 `pub fn run` 之前）加：

```rust
pub mod audio;
```

- [ ] **Step 2.8: 运行测试 → 应该 PASS**

Run:
```bash
cd app/src-tauri
cargo test
```

Expected: 4 tests pass.

- [ ] **Step 2.9: 定义 AppState 并注册 Tauri commands**

Modify `app/src-tauri/src/lib.rs`, 完整替换 `pub fn run` 及相关内容为：

```rust
pub mod audio;

use std::sync::Mutex;
use tauri::{Manager, State};

pub struct AppState {
    pub audio: Mutex<Option<audio::AudioPlayer>>,
}

#[tauri::command]
async fn audio_play(state: State<'_, AppState>, path: String) -> Result<(), String> {
    let guard = state.audio.lock().map_err(|e| e.to_string())?;
    let player = guard.as_ref().ok_or("audio not initialized".to_string())?;
    player
        .play_file(std::path::Path::new(&path))
        .map_err(|e| e.to_string())
}

#[tauri::command]
async fn audio_stop(state: State<'_, AppState>) -> Result<(), String> {
    let guard = state.audio.lock().map_err(|e| e.to_string())?;
    let player = guard.as_ref().ok_or("audio not initialized".to_string())?;
    player.stop();
    Ok(())
}

#[tauri::command]
async fn audio_is_playing(state: State<'_, AppState>) -> Result<bool, String> {
    let guard = state.audio.lock().map_err(|e| e.to_string())?;
    let player = guard.as_ref().ok_or("audio not initialized".to_string())?;
    Ok(player.is_playing())
}

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .setup(|app| {
            let player = audio::AudioPlayer::new().expect("audio init");
            app.manage(AppState {
                audio: Mutex::new(Some(player)),
            });
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![
            audio_play,
            audio_stop,
            audio_is_playing
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 2.10: 在 capabilities 里 allow 这三个命令**

Modify `app/src-tauri/capabilities/default.json`, 在 `permissions` 数组里加：

```json
"core:default",
"core:app:default"
```
(默认 template 里通常已经有 `core:default`。核心命令自动允许。自定义命令在 Tauri 2 里默认允许调用 handler 注册过的，无需额外 capability。) 

**Verification**: `pnpm dev` 能启动即可（若报权限错，参考 https://v2.tauri.app/security/capabilities/ 补配置。）

- [ ] **Step 2.11: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src-tauri" 
git commit -m "$(cat <<'EOF'
feat(lyra): rodio audio playback + Tauri commands (Sprint 0 T2)

- audio.rs with AudioPlayer::{new,play_file,stop,is_playing}
- audio_play/audio_stop/audio_is_playing Tauri commands
- 4 unit tests over silence_1s.wav fixture

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: TypeScript Audio Player Wrapper

**Files:**
- Create: `app/src/audio/player.ts`
- Create: `app/src/audio/player.test.ts`

**Interfaces Produced:**

```typescript
// audio/player.ts
export async function playFile(path: string): Promise<void>;
export async function stopPlayback(): Promise<void>;
export async function isPlaying(): Promise<boolean>;
```

**Interfaces Consumed:**
- Tauri commands from Task 2: `audio_play`, `audio_stop`, `audio_is_playing`

- [ ] **Step 3.1: 写 failing 测试**

Create `app/src/audio/player.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { playFile, stopPlayback, isPlaying } from "./player";

const invokeMock = vi.fn();

vi.mock("@tauri-apps/api/core", () => ({
  invoke: (...args: unknown[]) => invokeMock(...args),
}));

beforeEach(() => {
  invokeMock.mockReset();
});

describe("audio/player", () => {
  it("playFile calls audio_play command with path", async () => {
    invokeMock.mockResolvedValueOnce(undefined);
    await playFile("/tmp/song.mp3");
    expect(invokeMock).toHaveBeenCalledWith("audio_play", { path: "/tmp/song.mp3" });
  });

  it("stopPlayback calls audio_stop", async () => {
    invokeMock.mockResolvedValueOnce(undefined);
    await stopPlayback();
    expect(invokeMock).toHaveBeenCalledWith("audio_stop");
  });

  it("isPlaying returns boolean from audio_is_playing", async () => {
    invokeMock.mockResolvedValueOnce(true);
    await expect(isPlaying()).resolves.toBe(true);
    expect(invokeMock).toHaveBeenCalledWith("audio_is_playing");
  });

  it("playFile propagates errors from Rust", async () => {
    invokeMock.mockRejectedValueOnce("not initialized");
    await expect(playFile("/x")).rejects.toBe("not initialized");
  });
});
```

- [ ] **Step 3.2: 跑测试 → FAIL**

Run: `cd app && pnpm test src/audio/player.test.ts`
Expected: FAIL (`player.ts` not found)

- [ ] **Step 3.3: 实现 `player.ts`**

Create `app/src/audio/player.ts`:

```typescript
import { invoke } from "@tauri-apps/api/core";

export async function playFile(path: string): Promise<void> {
  await invoke("audio_play", { path });
}

export async function stopPlayback(): Promise<void> {
  await invoke("audio_stop");
}

export async function isPlaying(): Promise<boolean> {
  return await invoke<boolean>("audio_is_playing");
}
```

- [ ] **Step 3.4: 跑测试 → PASS**

Run: `pnpm test src/audio/player.test.ts`
Expected: 4 tests pass.

- [ ] **Step 3.5: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src/audio"
git commit -m "$(cat <<'EOF'
feat(lyra): TS audio player wrapper (Sprint 0 T3)

- playFile / stopPlayback / isPlaying wrappers around Tauri IPC
- 4 vitest cases w/ mocked invoke

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: SQLite Schema + Migrations

**Files:**
- Modify: `app/src-tauri/Cargo.toml` (add `tauri-plugin-sql` w/ `sqlite` feature)
- Modify: `app/package.json` (add `@tauri-apps/plugin-sql`)
- Create: `app/src-tauri/migrations/001_initial.sql`
- Modify: `app/src-tauri/src/lib.rs` (register plugin + migrations)
- Modify: `app/src-tauri/capabilities/default.json` (allow sql:default)
- Create: `app/src/db/client.ts`
- Create: `app/src/db/client.test.ts`

**Interfaces Produced:**

SQL schema (10 tables per spec §1.5):
- `dialogue_turns`, `emotion_snapshots`, `soul_state`, `shared_memory`,
- `library_tracks`, `library_features`,
- `roadmap`, `feature_requests`, `events`, `engineer_audit`

TypeScript:
```typescript
// db/client.ts
import Database from "@tauri-apps/plugin-sql";
export async function getDb(): Promise<Database>;
export const DB_URL: string;  // "sqlite:lyra.db"
```

**Interfaces Consumed:** 无

- [ ] **Step 4.1: 加 Rust dep**

Modify `app/src-tauri/Cargo.toml`, `[dependencies]`:

```toml
tauri-plugin-sql = { version = "2", features = ["sqlite"] }
```

- [ ] **Step 4.2: 加 TS dep**

Run:
```bash
cd app
pnpm add @tauri-apps/plugin-sql
```

- [ ] **Step 4.3: 创建 initial schema**

Create `app/src-tauri/migrations/001_initial.sql`:

```sql
-- 001_initial.sql — 音乐播放器 v0.1 骨架 schema
-- 对应 spec §1.5

CREATE TABLE dialogue_turns (
  id TEXT PRIMARY KEY,
  timestamp INTEGER NOT NULL,
  user_utterance_json TEXT NOT NULL,
  agent_response_json TEXT NOT NULL,
  user_reaction_json TEXT NOT NULL,
  current_emotion_json TEXT NOT NULL,
  emotion_delta_json TEXT NOT NULL
);
CREATE INDEX idx_turns_ts ON dialogue_turns(timestamp);

CREATE TABLE emotion_snapshots (
  id TEXT PRIMARY KEY,
  timestamp INTEGER NOT NULL,
  turn_id TEXT,
  pad_p REAL NOT NULL,
  pad_a REAL NOT NULL,
  pad_d REAL NOT NULL,
  labels_json TEXT NOT NULL,
  confidence REAL NOT NULL,
  source TEXT NOT NULL,
  FOREIGN KEY (turn_id) REFERENCES dialogue_turns(id)
);
CREATE INDEX idx_snap_ts ON emotion_snapshots(timestamp);

CREATE TABLE soul_state (
  agent_id TEXT PRIMARY KEY,
  created_at TEXT NOT NULL,
  taste_base_json TEXT NOT NULL,
  dynamic_mood_json TEXT NOT NULL,
  proactive_budget_json TEXT NOT NULL,
  updated_at INTEGER NOT NULL
);

CREATE TABLE shared_memory (
  id TEXT PRIMARY KEY,
  timestamp INTEGER NOT NULL,
  song_id TEXT NOT NULL,
  context TEXT NOT NULL,
  significance TEXT NOT NULL
);
CREATE INDEX idx_sm_ts ON shared_memory(timestamp);
CREATE INDEX idx_sm_song ON shared_memory(song_id);

CREATE TABLE library_tracks (
  id TEXT PRIMARY KEY,
  path TEXT NOT NULL UNIQUE,
  origin TEXT NOT NULL CHECK (origin IN ('local','web','generated')),
  title TEXT,
  artist TEXT,
  album TEXT,
  duration_ms INTEGER,
  added_at INTEGER NOT NULL,
  metadata_json TEXT
);
CREATE INDEX idx_tracks_origin ON library_tracks(origin);

CREATE TABLE library_features (
  track_id TEXT PRIMARY KEY,
  bpm REAL,
  energy REAL,
  valence REAL,
  tags_json TEXT,
  embedding_json TEXT,
  FOREIGN KEY (track_id) REFERENCES library_tracks(id) ON DELETE CASCADE
);

CREATE TABLE roadmap (
  id TEXT PRIMARY KEY,
  created_at INTEGER NOT NULL,
  created_by TEXT NOT NULL,
  title TEXT NOT NULL,
  rationale TEXT NOT NULL,
  evidence_json TEXT,
  proposed_change_json TEXT,
  status TEXT NOT NULL,
  priority INTEGER NOT NULL DEFAULT 50,
  effort TEXT
);
CREATE INDEX idx_road_status ON roadmap(status);

CREATE TABLE feature_requests (
  id TEXT PRIMARY KEY,
  created_at INTEGER NOT NULL,
  from_agent TEXT NOT NULL,
  desire TEXT NOT NULL,
  observed_pattern TEXT,
  urgency TEXT NOT NULL,
  consumed INTEGER NOT NULL DEFAULT 0
);

CREATE TABLE events (
  id TEXT PRIMARY KEY,
  timestamp INTEGER NOT NULL,
  kind TEXT NOT NULL,
  producer TEXT NOT NULL,
  payload_json TEXT NOT NULL,
  consumed_by_json TEXT
);
CREATE INDEX idx_events_kind ON events(kind);
CREATE INDEX idx_events_ts ON events(timestamp);

CREATE TABLE engineer_audit (
  id TEXT PRIMARY KEY,
  timestamp INTEGER NOT NULL,
  task_id TEXT NOT NULL,
  phase TEXT NOT NULL,
  payload_json TEXT NOT NULL
);
CREATE INDEX idx_audit_task ON engineer_audit(task_id);
```

- [ ] **Step 4.4: 注册 sql plugin + migrations**

Modify `app/src-tauri/src/lib.rs`, 在 `pub fn run` 顶部（Builder 前）加：

```rust
use tauri_plugin_sql::{Migration, MigrationKind};
```

并在 `tauri::Builder::default()` 后立即 chain:

```rust
.plugin(
    tauri_plugin_sql::Builder::default()
        .add_migrations(
            "sqlite:lyra.db",
            vec![Migration {
                version: 1,
                description: "initial schema",
                sql: include_str!("../migrations/001_initial.sql"),
                kind: MigrationKind::Up,
            }],
        )
        .build(),
)
```

（放在 `.setup(...)` 之前。）

- [ ] **Step 4.5: 添加 capability**

Modify `app/src-tauri/capabilities/default.json`, 在 `permissions` 数组里加：
```json
"sql:default",
"sql:allow-execute"
```

- [ ] **Step 4.6: 写 TS db/client 测试**

Create `app/src/db/client.test.ts`:

```typescript
import { describe, it, expect, vi } from "vitest";

const loadMock = vi.fn();

vi.mock("@tauri-apps/plugin-sql", () => ({
  default: { load: (...args: unknown[]) => loadMock(...args) },
}));

import { getDb, DB_URL } from "./client";

describe("db/client", () => {
  it("DB_URL is sqlite:lyra.db", () => {
    expect(DB_URL).toBe("sqlite:lyra.db");
  });

  it("getDb loads via plugin with DB_URL", async () => {
    const fake = { execute: vi.fn(), select: vi.fn() };
    loadMock.mockResolvedValueOnce(fake);
    const db = await getDb();
    expect(loadMock).toHaveBeenCalledWith("sqlite:lyra.db");
    expect(db).toBe(fake);
  });

  it("getDb memoizes the connection", async () => {
    loadMock.mockClear();
    const fake = { execute: vi.fn(), select: vi.fn() };
    loadMock.mockResolvedValue(fake);
    const a = await getDb();
    const b = await getDb();
    expect(a).toBe(b);
    // NOTE: memoization can span tests; ensure loadMock was invoked at most once
    // in this describe execution (0 acceptable if prior test already loaded)
    expect(loadMock.mock.calls.length).toBeLessThanOrEqual(1);
  });
});
```

- [ ] **Step 4.7: 跑测试 → FAIL**

Run: `pnpm test src/db/client.test.ts`
Expected: FAIL (`client.ts` not found)

- [ ] **Step 4.8: 实现 db/client.ts**

Create `app/src/db/client.ts`:

```typescript
import Database from "@tauri-apps/plugin-sql";

export const DB_URL = "sqlite:lyra.db";

let _db: Database | null = null;

export async function getDb(): Promise<Database> {
  if (_db) return _db;
  _db = await Database.load(DB_URL);
  return _db;
}
```

- [ ] **Step 4.9: 跑测试 → PASS**

Run: `pnpm test src/db/client.test.ts`
Expected: 3 tests pass.

- [ ] **Step 4.10: 手动 verify 迁移成功**

Run:
```bash
cd app
pnpm dev
# 让窗口开着，然后另开一个 terminal:
ls -la ~/Library/Application\ Support/com.daoyu.lyra/
```

Expected: `lyra.db` 文件存在。检查 tables:

```bash
sqlite3 ~/Library/Application\ Support/com.daoyu.lyra/lyra.db ".tables"
```

Expected 输出包含 10 张表名：`dialogue_turns emotion_snapshots events feature_requests engineer_audit library_features library_tracks roadmap shared_memory soul_state`

关闭 dev 窗口。

- [ ] **Step 4.11: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src-tauri" "app/src/db" "app/package.json" "app/pnpm-lock.yaml"
git commit -m "$(cat <<'EOF'
feat(lyra): SQLite schema + tauri-plugin-sql (Sprint 0 T4)

- 10 tables per spec §1.5 as migration 001_initial.sql
- TS db/client.ts wrapper w/ memoized Database.load
- 3 vitest cases

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Core TypeScript Type Definitions

**Files:**
- Create: `app/src/types/dialogue.ts`
- Create: `app/src/types/soul.ts`
- Create: `app/src/types/song.ts`
- Create: `app/src/types/provider.ts`
- Create: `app/src/types/index.ts`

**Interfaces Produced:** 参见 spec §2.1 和 §2.2 的完整 TS 类型（包括 `DialogueTurn`、`SoulState`、`Song`、`ModelProvider`、`ChatMessage`、`ChatResponse` 等）。

**Interfaces Consumed:** 无（纯类型定义）

- [ ] **Step 5.1: 创建 `types/dialogue.ts`**

```typescript
// types/dialogue.ts — DialogueTurn 及相关（spec §2.1）
export type PAD = { p: number; a: number; d: number };

export type CurrentEmotion = {
  pad: PAD;
  labels: string[];
  confidence: number;
  source: "emotion-agent-inferred" | "user-declared" | "ring-signal";
  predicted_trajectory?: {
    horizon_min: number;
    predicted_pad: PAD;
  };
};

export type UserUtterance = {
  modality: "text" | "voice" | "proactive-open";
  content: string;
};

export type ProactiveKind =
  | "morning"
  | "care"
  | "anniversary"
  | "share"
  | "rhythm";

export type AgentResponse = {
  song_id: string;
  rationale: string;
  proactive_kind?: ProactiveKind;
  generation_meta?: {
    generator: string;
    prompt: string;
    duration_ms: number;
  };
};

export type UserReaction = {
  behavioral: {
    listen_duration_ms: number;
    completed: boolean;
    skipped: boolean;
    repeated: number;
    volume_delta: number;
  };
  verbal?: {
    content: string;
    parsed_valence: "positive" | "negative" | "neutral";
  };
  silence_positive: boolean;
};

export type DialogueTurn = {
  id: string;
  timestamp: number;
  current_emotion: CurrentEmotion;
  user_utterance: UserUtterance;
  agent_response: AgentResponse;
  user_reaction: UserReaction;
  emotion_delta: PAD;
};
```

- [ ] **Step 5.2: 创建 `types/soul.ts`**

```typescript
// types/soul.ts — SoulState（spec §2.2）
import type { PAD, ProactiveKind } from "./dialogue";

export type AestheticAxes = {
  restraint_vs_expression: number;
  narrative_vs_atmospheric: number;
  polished_vs_raw: number;
  novelty_seeking: number;
};

export type MusicalTasteBase = {
  aesthetic_axes: AestheticAxes;
  affinity_genres: string[];
  aversion_signals: string[];
  backbone: string;
};

export type DynamicMood = {
  current_pad: PAD;
  attention_to_user: number;
  recent_bias: string;
};

export type SharedMemoryEntry = {
  timestamp: string;
  song_id: string;
  context: string;
  significance: string;
};

export type EvolutionLogEntry = {
  quarter: string;
  summary: string;
  adjustment: string;
  rollback_id: string;
};

export type ProactiveBudget = {
  daily_limit: number;
  sulk_until: string | null;
  kind_budgets: Record<ProactiveKind, number>;
};

export type SoulState = {
  agent_id: string;
  created_at: string;
  musical_taste_base: MusicalTasteBase;
  dynamic_mood: DynamicMood;
  shared_memory: SharedMemoryEntry[];
  evolution_log: EvolutionLogEntry[];
  proactive_budget: ProactiveBudget;
};
```

- [ ] **Step 5.3: 创建 `types/song.ts`**

```typescript
// types/song.ts — 曲库对象
export type SongOrigin = "local" | "web" | "generated";

export type LibraryTrack = {
  id: string;
  path: string;
  origin: SongOrigin;
  title?: string;
  artist?: string;
  album?: string;
  duration_ms?: number;
  added_at: number;
  metadata?: Record<string, unknown>;
};

export type TrackFeatures = {
  track_id: string;
  bpm?: number;
  energy?: number;
  valence?: number;
  tags?: string[];
  embedding?: number[];
};
```

- [ ] **Step 5.4: 创建 `types/provider.ts`**

```typescript
// types/provider.ts — Model Provider 抽象层
export type ChatRole = "system" | "user" | "assistant";

export type ChatMessage = {
  role: ChatRole;
  content: string;
};

export type ChatOptions = {
  model?: string;
  max_tokens?: number;
  temperature?: number;
};

export type ChatResponse = {
  content: string;
  usage?: {
    input_tokens: number;
    output_tokens: number;
  };
  raw?: unknown;
};

export type ProviderId =
  | "anthropic"
  | "deepseek"
  | "zhipu"
  | "doubao"
  | "openai"
  | "local-ollama";

export interface ModelProvider {
  readonly id: ProviderId;
  chat(messages: ChatMessage[], opts?: ChatOptions): Promise<ChatResponse>;
  embed?(text: string): Promise<number[]>;
}
```

- [ ] **Step 5.5: barrel `types/index.ts`**

```typescript
export * from "./dialogue";
export * from "./soul";
export * from "./song";
export * from "./provider";
```

- [ ] **Step 5.6: typecheck**

Run:
```bash
cd app
pnpm typecheck
```

Expected: 0 errors.

- [ ] **Step 5.7: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src/types"
git commit -m "$(cat <<'EOF'
feat(lyra): core TS type definitions (Sprint 0 T5)

- dialogue.ts: DialogueTurn, PAD, CurrentEmotion, ProactiveKind
- soul.ts: SoulState, MusicalTasteBase, DynamicMood
- song.ts: LibraryTrack, TrackFeatures
- provider.ts: ModelProvider interface, ChatMessage, ChatResponse

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Model Provider Registry

**Files:**
- Create: `app/src/providers/registry.ts`
- Create: `app/src/providers/registry.test.ts`

**Interfaces Produced:**

```typescript
export class ProviderRegistry {
  register(p: ModelProvider): void;
  get(id: ProviderId): ModelProvider;   // throws if missing
  has(id: ProviderId): boolean;
  list(): ProviderId[];
}
export const registry: ProviderRegistry; // singleton
```

**Interfaces Consumed:**
- `ModelProvider`, `ProviderId` from Task 5 `types/provider.ts`

- [ ] **Step 6.1: 写 failing 测试**

Create `app/src/providers/registry.test.ts`:

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import { ProviderRegistry } from "./registry";
import type { ModelProvider, ChatMessage } from "../types";

const fake = (id: any): ModelProvider => ({
  id,
  async chat(_: ChatMessage[]) {
    return { content: "" };
  },
});

describe("ProviderRegistry", () => {
  let r: ProviderRegistry;
  beforeEach(() => {
    r = new ProviderRegistry();
  });

  it("registers and retrieves by id", () => {
    const p = fake("anthropic");
    r.register(p);
    expect(r.get("anthropic")).toBe(p);
  });

  it("throws when getting an unregistered id", () => {
    expect(() => r.get("openai")).toThrow(/openai/);
  });

  it("has() returns boolean without throwing", () => {
    expect(r.has("anthropic")).toBe(false);
    r.register(fake("anthropic"));
    expect(r.has("anthropic")).toBe(true);
  });

  it("list() returns registered ids", () => {
    r.register(fake("anthropic"));
    r.register(fake("deepseek"));
    expect(r.list().sort()).toEqual(["anthropic", "deepseek"]);
  });

  it("re-registering same id overwrites", () => {
    const a = fake("anthropic");
    const b = fake("anthropic");
    r.register(a);
    r.register(b);
    expect(r.get("anthropic")).toBe(b);
  });
});
```

- [ ] **Step 6.2: 跑测试 → FAIL**

Run: `pnpm test src/providers/registry.test.ts`
Expected: FAIL (module not found)

- [ ] **Step 6.3: 实现 `registry.ts`**

Create `app/src/providers/registry.ts`:

```typescript
import type { ModelProvider, ProviderId } from "../types";

export class ProviderRegistry {
  private providers = new Map<ProviderId, ModelProvider>();

  register(p: ModelProvider): void {
    this.providers.set(p.id, p);
  }

  get(id: ProviderId): ModelProvider {
    const p = this.providers.get(id);
    if (!p) throw new Error(`provider not registered: ${id}`);
    return p;
  }

  has(id: ProviderId): boolean {
    return this.providers.has(id);
  }

  list(): ProviderId[] {
    return [...this.providers.keys()];
  }
}

export const registry = new ProviderRegistry();
```

- [ ] **Step 6.4: 跑测试 → PASS**

Run: `pnpm test src/providers/registry.test.ts`
Expected: 5 tests pass.

- [ ] **Step 6.5: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src/providers"
git commit -m "$(cat <<'EOF'
feat(lyra): ProviderRegistry (Sprint 0 T6)

- register/get/has/list, throws on missing get
- singleton export
- 5 vitest cases

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Anthropic Provider Adapter

**Files:**
- Create: `app/src/providers/anthropic.ts`
- Create: `app/src/providers/anthropic.test.ts`

**Interfaces Produced:**

```typescript
export class AnthropicProvider implements ModelProvider {
  readonly id = "anthropic";
  constructor(opts: { apiKey: string; model?: string; baseUrl?: string });
  chat(messages: ChatMessage[], opts?: ChatOptions): Promise<ChatResponse>;
}
```

**Interfaces Consumed:**
- `ModelProvider`, `ChatMessage`, `ChatResponse`, `ChatOptions` from `types/provider.ts`

**API reference (2026-01 cutoff):** Anthropic Messages API `POST https://api.anthropic.com/v1/messages`, headers `x-api-key`, `anthropic-version: 2023-06-01`, `content-type: application/json`. Default model `claude-opus-4-7`（per spec §3.5）。

- [ ] **Step 7.1: 写 failing 测试**

Create `app/src/providers/anthropic.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { AnthropicProvider } from "./anthropic";

describe("AnthropicProvider", () => {
  const originalFetch = global.fetch;
  const fetchMock = vi.fn();

  beforeEach(() => {
    fetchMock.mockReset();
    global.fetch = fetchMock as unknown as typeof fetch;
  });

  afterEach(() => {
    global.fetch = originalFetch;
  });

  it("has id 'anthropic'", () => {
    const p = new AnthropicProvider({ apiKey: "sk-x" });
    expect(p.id).toBe("anthropic");
  });

  it("posts messages to /v1/messages with x-api-key", async () => {
    fetchMock.mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        content: [{ type: "text", text: "hi" }],
        usage: { input_tokens: 3, output_tokens: 1 },
      }),
    });

    const p = new AnthropicProvider({ apiKey: "sk-abc", model: "claude-opus-4-7" });
    const res = await p.chat([
      { role: "user", content: "hello" },
    ]);

    expect(fetchMock).toHaveBeenCalledOnce();
    const [url, init] = fetchMock.mock.calls[0];
    expect(url).toBe("https://api.anthropic.com/v1/messages");
    expect((init as any).method).toBe("POST");
    expect((init as any).headers["x-api-key"]).toBe("sk-abc");
    expect((init as any).headers["anthropic-version"]).toBe("2023-06-01");
    const body = JSON.parse((init as any).body);
    expect(body.model).toBe("claude-opus-4-7");
    expect(body.messages).toEqual([{ role: "user", content: "hello" }]);
    expect(body.max_tokens).toBeGreaterThan(0);

    expect(res.content).toBe("hi");
    expect(res.usage).toEqual({ input_tokens: 3, output_tokens: 1 });
  });

  it("throws on non-ok response with body text", async () => {
    fetchMock.mockResolvedValueOnce({
      ok: false,
      status: 401,
      text: async () => "bad key",
    });
    const p = new AnthropicProvider({ apiKey: "sk-bad" });
    await expect(p.chat([{ role: "user", content: "hi" }])).rejects.toThrow(/401/);
  });

  it("passes system messages via top-level 'system' field", async () => {
    fetchMock.mockResolvedValueOnce({
      ok: true,
      json: async () => ({ content: [{ type: "text", text: "ok" }] }),
    });
    const p = new AnthropicProvider({ apiKey: "sk-x" });
    await p.chat([
      { role: "system", content: "you are lyra" },
      { role: "user", content: "hi" },
    ]);
    const body = JSON.parse((fetchMock.mock.calls[0][1] as any).body);
    expect(body.system).toBe("you are lyra");
    expect(body.messages).toEqual([{ role: "user", content: "hi" }]);
  });
});
```

Add missing import at top: `import { afterEach } from "vitest";`

- [ ] **Step 7.2: 跑测试 → FAIL**

Run: `pnpm test src/providers/anthropic.test.ts`
Expected: FAIL (module not found).

- [ ] **Step 7.3: 实现 `anthropic.ts`**

Create `app/src/providers/anthropic.ts`:

```typescript
import type {
  ModelProvider,
  ChatMessage,
  ChatOptions,
  ChatResponse,
} from "../types";

const DEFAULT_MODEL = "claude-opus-4-7";
const DEFAULT_MAX_TOKENS = 1024;

export class AnthropicProvider implements ModelProvider {
  readonly id = "anthropic" as const;
  private apiKey: string;
  private baseUrl: string;
  private defaultModel: string;

  constructor(opts: { apiKey: string; model?: string; baseUrl?: string }) {
    if (!opts.apiKey) throw new Error("AnthropicProvider requires apiKey");
    this.apiKey = opts.apiKey;
    this.baseUrl = opts.baseUrl ?? "https://api.anthropic.com";
    this.defaultModel = opts.model ?? DEFAULT_MODEL;
  }

  async chat(messages: ChatMessage[], opts?: ChatOptions): Promise<ChatResponse> {
    const systemParts = messages
      .filter((m) => m.role === "system")
      .map((m) => m.content);
    const nonSystem = messages.filter((m) => m.role !== "system");

    const body: Record<string, unknown> = {
      model: opts?.model ?? this.defaultModel,
      max_tokens: opts?.max_tokens ?? DEFAULT_MAX_TOKENS,
      messages: nonSystem.map((m) => ({ role: m.role, content: m.content })),
    };
    if (systemParts.length > 0) body.system = systemParts.join("\n\n");
    if (opts?.temperature != null) body.temperature = opts.temperature;

    const res = await fetch(`${this.baseUrl}/v1/messages`, {
      method: "POST",
      headers: {
        "x-api-key": this.apiKey,
        "anthropic-version": "2023-06-01",
        "content-type": "application/json",
      },
      body: JSON.stringify(body),
    });

    if (!res.ok) {
      const text = await res.text();
      throw new Error(`Anthropic ${res.status}: ${text}`);
    }

    const data = await res.json();
    const content = (data.content ?? [])
      .filter((b: { type: string }) => b.type === "text")
      .map((b: { text: string }) => b.text)
      .join("");

    return {
      content,
      usage: data.usage,
      raw: data,
    };
  }
}
```

- [ ] **Step 7.4: 跑测试 → PASS**

Run: `pnpm test src/providers/anthropic.test.ts`
Expected: 4 tests pass.

- [ ] **Step 7.5: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src/providers/anthropic.ts" "app/src/providers/anthropic.test.ts"
git commit -m "$(cat <<'EOF'
feat(lyra): AnthropicProvider adapter (Sprint 0 T7)

- POST /v1/messages with x-api-key + anthropic-version
- system role folded into top-level 'system' field
- 4 vitest cases w/ mocked fetch

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: DeepSeek Provider Adapter

**Files:**
- Create: `app/src/providers/deepseek.ts`
- Create: `app/src/providers/deepseek.test.ts`

**Interfaces Produced:**

```typescript
export class DeepSeekProvider implements ModelProvider {
  readonly id = "deepseek";
  constructor(opts: { apiKey: string; model?: string; baseUrl?: string });
  chat(messages: ChatMessage[], opts?: ChatOptions): Promise<ChatResponse>;
}
```

**Interfaces Consumed:** 同 Task 7

**API reference:** DeepSeek 采用 OpenAI 兼容格式，`POST https://api.deepseek.com/v1/chat/completions`, `Authorization: Bearer <key>`, model 默认 `deepseek-chat`。

- [ ] **Step 8.1: 写 failing 测试**

Create `app/src/providers/deepseek.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { DeepSeekProvider } from "./deepseek";

describe("DeepSeekProvider", () => {
  const originalFetch = global.fetch;
  const fetchMock = vi.fn();
  beforeEach(() => {
    fetchMock.mockReset();
    global.fetch = fetchMock as unknown as typeof fetch;
  });
  afterEach(() => {
    global.fetch = originalFetch;
  });

  it("id is 'deepseek'", () => {
    expect(new DeepSeekProvider({ apiKey: "x" }).id).toBe("deepseek");
  });

  it("posts to /v1/chat/completions with Bearer", async () => {
    fetchMock.mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        choices: [{ message: { role: "assistant", content: "hi" } }],
        usage: { prompt_tokens: 3, completion_tokens: 1 },
      }),
    });
    const p = new DeepSeekProvider({ apiKey: "sk-d" });
    const res = await p.chat([{ role: "user", content: "hello" }]);

    const [url, init] = fetchMock.mock.calls[0];
    expect(url).toBe("https://api.deepseek.com/v1/chat/completions");
    expect((init as any).headers["authorization"]).toBe("Bearer sk-d");
    const body = JSON.parse((init as any).body);
    expect(body.model).toBe("deepseek-chat");
    expect(body.messages).toEqual([{ role: "user", content: "hello" }]);

    expect(res.content).toBe("hi");
    expect(res.usage).toEqual({ input_tokens: 3, output_tokens: 1 });
  });

  it("preserves system role in the messages array", async () => {
    fetchMock.mockResolvedValueOnce({
      ok: true,
      json: async () => ({ choices: [{ message: { content: "ok" } }] }),
    });
    const p = new DeepSeekProvider({ apiKey: "x" });
    await p.chat([
      { role: "system", content: "sys" },
      { role: "user", content: "u" },
    ]);
    const body = JSON.parse((fetchMock.mock.calls[0][1] as any).body);
    expect(body.messages).toEqual([
      { role: "system", content: "sys" },
      { role: "user", content: "u" },
    ]);
  });

  it("throws on non-ok status", async () => {
    fetchMock.mockResolvedValueOnce({
      ok: false,
      status: 429,
      text: async () => "rate limit",
    });
    const p = new DeepSeekProvider({ apiKey: "x" });
    await expect(p.chat([{ role: "user", content: "hi" }])).rejects.toThrow(/429/);
  });
});
```

- [ ] **Step 8.2: 跑测试 → FAIL**

Run: `pnpm test src/providers/deepseek.test.ts`
Expected: FAIL

- [ ] **Step 8.3: 实现 `deepseek.ts`**

Create `app/src/providers/deepseek.ts`:

```typescript
import type {
  ModelProvider,
  ChatMessage,
  ChatOptions,
  ChatResponse,
} from "../types";

const DEFAULT_MODEL = "deepseek-chat";

export class DeepSeekProvider implements ModelProvider {
  readonly id = "deepseek" as const;
  private apiKey: string;
  private baseUrl: string;
  private defaultModel: string;

  constructor(opts: { apiKey: string; model?: string; baseUrl?: string }) {
    if (!opts.apiKey) throw new Error("DeepSeekProvider requires apiKey");
    this.apiKey = opts.apiKey;
    this.baseUrl = opts.baseUrl ?? "https://api.deepseek.com";
    this.defaultModel = opts.model ?? DEFAULT_MODEL;
  }

  async chat(messages: ChatMessage[], opts?: ChatOptions): Promise<ChatResponse> {
    const body: Record<string, unknown> = {
      model: opts?.model ?? this.defaultModel,
      messages: messages.map((m) => ({ role: m.role, content: m.content })),
    };
    if (opts?.max_tokens != null) body.max_tokens = opts.max_tokens;
    if (opts?.temperature != null) body.temperature = opts.temperature;

    const res = await fetch(`${this.baseUrl}/v1/chat/completions`, {
      method: "POST",
      headers: {
        authorization: `Bearer ${this.apiKey}`,
        "content-type": "application/json",
      },
      body: JSON.stringify(body),
    });

    if (!res.ok) {
      const text = await res.text();
      throw new Error(`DeepSeek ${res.status}: ${text}`);
    }

    const data = await res.json();
    const content: string = data.choices?.[0]?.message?.content ?? "";
    const usage = data.usage
      ? {
          input_tokens: data.usage.prompt_tokens ?? 0,
          output_tokens: data.usage.completion_tokens ?? 0,
        }
      : undefined;

    return { content, usage, raw: data };
  }
}
```

- [ ] **Step 8.4: 跑测试 → PASS**

Run: `pnpm test src/providers/deepseek.test.ts`
Expected: 4 tests pass.

- [ ] **Step 8.5: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src/providers/deepseek.ts" "app/src/providers/deepseek.test.ts"
git commit -m "$(cat <<'EOF'
feat(lyra): DeepSeekProvider adapter (Sprint 0 T8)

- POST /v1/chat/completions with Bearer, OpenAI-compat shape
- 4 vitest cases

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Keychain-Based Secret Storage

**Files:**
- Modify: `app/src-tauri/Cargo.toml` (add `keyring = "3"`)
- Create: `app/src-tauri/src/secrets.rs`
- Modify: `app/src-tauri/src/lib.rs` (register secret commands)
- Create: `app/src-tauri/tests/secrets_test.rs`
- Create: `app/src/settings/secrets.ts`
- Create: `app/src/settings/secrets.test.ts`

**Interfaces Produced:**

Rust:
```rust
pub fn set_secret(key: &str, value: &str) -> Result<(), keyring::Error>;
pub fn get_secret(key: &str) -> Result<Option<String>, keyring::Error>;
pub fn delete_secret(key: &str) -> Result<(), keyring::Error>;
```

Tauri commands:
```rust
#[tauri::command] async fn secret_set(key: String, value: String) -> Result<(), String>;
#[tauri::command] async fn secret_get(key: String) -> Result<Option<String>, String>;
#[tauri::command] async fn secret_delete(key: String) -> Result<(), String>;
```

TS:
```typescript
export async function setSecret(key: string, value: string): Promise<void>;
export async function getSecret(key: string): Promise<string | null>;
export async function deleteSecret(key: string): Promise<void>;

// convention:
export const SECRET_KEYS = {
  anthropicApiKey: "provider.anthropic.apiKey",
  deepseekApiKey: "provider.deepseek.apiKey",
};
```

**Interfaces Consumed:** 无

- [ ] **Step 9.1: 加 Rust dep**

Modify `app/src-tauri/Cargo.toml`:

```toml
keyring = "3"
```

- [ ] **Step 9.2: Rust 测试（先失败）**

Create `app/src-tauri/tests/secrets_test.rs`:

```rust
use lyra_lib::secrets;

// keyring test uses the mock keyring in tests (feature default in v3);
// on macOS CI this may fall through to the real Keychain; scope test keys
// with a random suffix to avoid pollution.

fn tkey(name: &str) -> String {
    format!("lyra-test-{}-{}", name, std::process::id())
}

#[test]
fn set_get_roundtrip() {
    let k = tkey("k1");
    secrets::set_secret(&k, "v1").unwrap();
    assert_eq!(secrets::get_secret(&k).unwrap().as_deref(), Some("v1"));
    secrets::delete_secret(&k).unwrap();
}

#[test]
fn get_missing_returns_none() {
    let k = tkey("does-not-exist");
    assert_eq!(secrets::get_secret(&k).unwrap(), None);
}

#[test]
fn delete_missing_is_ok() {
    let k = tkey("also-missing");
    secrets::delete_secret(&k).unwrap();
}
```

- [ ] **Step 9.3: 跑测试 → FAIL**

Run: `cd app/src-tauri && cargo test --test secrets_test`
Expected: FAIL (module not found).

- [ ] **Step 9.4: 实现 `secrets.rs`**

Create `app/src-tauri/src/secrets.rs`:

```rust
use keyring::Entry;

const SERVICE: &str = "com.daoyu.lyra";

fn entry(key: &str) -> Result<Entry, keyring::Error> {
    Entry::new(SERVICE, key)
}

pub fn set_secret(key: &str, value: &str) -> Result<(), keyring::Error> {
    entry(key)?.set_password(value)
}

pub fn get_secret(key: &str) -> Result<Option<String>, keyring::Error> {
    match entry(key)?.get_password() {
        Ok(v) => Ok(Some(v)),
        Err(keyring::Error::NoEntry) => Ok(None),
        Err(e) => Err(e),
    }
}

pub fn delete_secret(key: &str) -> Result<(), keyring::Error> {
    match entry(key)?.delete_credential() {
        Ok(()) => Ok(()),
        Err(keyring::Error::NoEntry) => Ok(()),
        Err(e) => Err(e),
    }
}
```

- [ ] **Step 9.5: 导出模块 + 注册 Tauri commands**

Modify `app/src-tauri/src/lib.rs`:

在顶部（audio 之后）加：
```rust
pub mod secrets;
```

在原有 audio commands 之下（`pub fn run` 之前）加：

```rust
#[tauri::command]
async fn secret_set(key: String, value: String) -> Result<(), String> {
    secrets::set_secret(&key, &value).map_err(|e| e.to_string())
}

#[tauri::command]
async fn secret_get(key: String) -> Result<Option<String>, String> {
    secrets::get_secret(&key).map_err(|e| e.to_string())
}

#[tauri::command]
async fn secret_delete(key: String) -> Result<(), String> {
    secrets::delete_secret(&key).map_err(|e| e.to_string())
}
```

修改 `invoke_handler` 加上这三个：

```rust
.invoke_handler(tauri::generate_handler![
    audio_play, audio_stop, audio_is_playing,
    secret_set, secret_get, secret_delete
])
```

- [ ] **Step 9.6: 跑 Rust 测试 → PASS**

Run: `cd app/src-tauri && cargo test`
Expected: 全部 pass（audio 4 + secrets 3）。

macOS 上第一次运行 keyring 测试可能弹出授权提示，Allow 一次即可。

- [ ] **Step 9.7: TS 侧 secrets 测试**

Create `app/src/settings/secrets.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";

const invokeMock = vi.fn();
vi.mock("@tauri-apps/api/core", () => ({
  invoke: (...args: unknown[]) => invokeMock(...args),
}));

import { setSecret, getSecret, deleteSecret, SECRET_KEYS } from "./secrets";

beforeEach(() => invokeMock.mockReset());

describe("settings/secrets", () => {
  it("setSecret calls secret_set", async () => {
    invokeMock.mockResolvedValueOnce(undefined);
    await setSecret("k", "v");
    expect(invokeMock).toHaveBeenCalledWith("secret_set", { key: "k", value: "v" });
  });

  it("getSecret returns null when Rust returns null", async () => {
    invokeMock.mockResolvedValueOnce(null);
    await expect(getSecret("k")).resolves.toBeNull();
  });

  it("getSecret returns string when Rust returns string", async () => {
    invokeMock.mockResolvedValueOnce("v");
    await expect(getSecret("k")).resolves.toBe("v");
  });

  it("deleteSecret calls secret_delete", async () => {
    invokeMock.mockResolvedValueOnce(undefined);
    await deleteSecret("k");
    expect(invokeMock).toHaveBeenCalledWith("secret_delete", { key: "k" });
  });

  it("exports SECRET_KEYS with anthropic/deepseek entries", () => {
    expect(SECRET_KEYS.anthropicApiKey).toBe("provider.anthropic.apiKey");
    expect(SECRET_KEYS.deepseekApiKey).toBe("provider.deepseek.apiKey");
  });
});
```

- [ ] **Step 9.8: 跑测试 → FAIL**

Run: `pnpm test src/settings/secrets.test.ts`
Expected: FAIL (module not found).

- [ ] **Step 9.9: 实现 TS `secrets.ts`**

Create `app/src/settings/secrets.ts`:

```typescript
import { invoke } from "@tauri-apps/api/core";

export const SECRET_KEYS = {
  anthropicApiKey: "provider.anthropic.apiKey",
  deepseekApiKey: "provider.deepseek.apiKey",
} as const;

export async function setSecret(key: string, value: string): Promise<void> {
  await invoke("secret_set", { key, value });
}

export async function getSecret(key: string): Promise<string | null> {
  const v = await invoke<string | null>("secret_get", { key });
  return v ?? null;
}

export async function deleteSecret(key: string): Promise<void> {
  await invoke("secret_delete", { key });
}
```

- [ ] **Step 9.10: 跑测试 → PASS**

Run: `pnpm test src/settings/secrets.test.ts`
Expected: 5 tests pass.

- [ ] **Step 9.11: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src-tauri" "app/src/settings"
git commit -m "$(cat <<'EOF'
feat(lyra): keyring-backed secret storage (Sprint 0 T9)

- Rust secrets module w/ set/get/delete + NoEntry-safe semantics
- Tauri secret_set/get/delete commands
- TS wrapper + SECRET_KEYS convention
- 3 Rust + 5 TS tests

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: Settings Modal (React)

**Files:**
- Create: `app/src/settings/Settings.tsx`
- Create: `app/src/settings/Settings.test.tsx`
- Modify: `app/src/App.tsx` (mount Settings via a button)

**Interfaces Produced:**

```tsx
export type SettingsProps = {
  open: boolean;
  onClose: () => void;
};
export function Settings(props: SettingsProps): JSX.Element;
```

**Interfaces Consumed:**
- `setSecret`, `getSecret`, `SECRET_KEYS` from Task 9

- [ ] **Step 10.1: 写 failing 测试**

Create `app/src/settings/Settings.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { Settings } from "./Settings";

vi.mock("./secrets", () => ({
  SECRET_KEYS: {
    anthropicApiKey: "provider.anthropic.apiKey",
    deepseekApiKey: "provider.deepseek.apiKey",
  },
  setSecret: vi.fn(),
  getSecret: vi.fn(),
  deleteSecret: vi.fn(),
}));

import { setSecret, getSecret } from "./secrets";

beforeEach(() => {
  (setSecret as any).mockReset();
  (getSecret as any).mockReset();
});

describe("Settings", () => {
  it("does not render when closed", () => {
    render(<Settings open={false} onClose={() => {}} />);
    expect(screen.queryByLabelText(/anthropic/i)).toBeNull();
  });

  it("loads existing secrets on open", async () => {
    (getSecret as any).mockImplementation((k: string) =>
      k === "provider.anthropic.apiKey" ? Promise.resolve("sk-a") : Promise.resolve(null)
    );
    render(<Settings open={true} onClose={() => {}} />);
    await waitFor(() =>
      expect((screen.getByLabelText(/anthropic/i) as HTMLInputElement).value).toBe("sk-a")
    );
  });

  it("saves both secrets on Save click and calls onClose", async () => {
    (getSecret as any).mockResolvedValue(null);
    (setSecret as any).mockResolvedValue(undefined);
    const onClose = vi.fn();
    render(<Settings open={true} onClose={onClose} />);

    const aInput = await screen.findByLabelText(/anthropic/i);
    const dInput = screen.getByLabelText(/deepseek/i);
    fireEvent.change(aInput, { target: { value: "sk-new-a" } });
    fireEvent.change(dInput, { target: { value: "sk-new-d" } });

    fireEvent.click(screen.getByRole("button", { name: /save/i }));

    await waitFor(() => {
      expect(setSecret).toHaveBeenCalledWith("provider.anthropic.apiKey", "sk-new-a");
      expect(setSecret).toHaveBeenCalledWith("provider.deepseek.apiKey", "sk-new-d");
      expect(onClose).toHaveBeenCalled();
    });
  });

  it("Cancel button closes without saving", async () => {
    (getSecret as any).mockResolvedValue(null);
    const onClose = vi.fn();
    render(<Settings open={true} onClose={onClose} />);
    await screen.findByLabelText(/anthropic/i);
    fireEvent.click(screen.getByRole("button", { name: /cancel/i }));
    expect(onClose).toHaveBeenCalled();
    expect(setSecret).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 10.2: 跑测试 → FAIL**

Run: `pnpm test src/settings/Settings.test.tsx`
Expected: FAIL (module not found).

- [ ] **Step 10.3: 实现 `Settings.tsx`**

Create `app/src/settings/Settings.tsx`:

```tsx
import { useEffect, useState } from "react";
import { SECRET_KEYS, setSecret, getSecret } from "./secrets";

export type SettingsProps = {
  open: boolean;
  onClose: () => void;
};

export function Settings({ open, onClose }: SettingsProps) {
  const [anthropic, setAnthropic] = useState("");
  const [deepseek, setDeepseek] = useState("");
  const [loaded, setLoaded] = useState(false);
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    if (!open) return;
    let cancelled = false;
    (async () => {
      const [a, d] = await Promise.all([
        getSecret(SECRET_KEYS.anthropicApiKey),
        getSecret(SECRET_KEYS.deepseekApiKey),
      ]);
      if (cancelled) return;
      setAnthropic(a ?? "");
      setDeepseek(d ?? "");
      setLoaded(true);
    })();
    return () => {
      cancelled = true;
    };
  }, [open]);

  if (!open) return null;

  const onSave = async () => {
    setSaving(true);
    try {
      await setSecret(SECRET_KEYS.anthropicApiKey, anthropic);
      await setSecret(SECRET_KEYS.deepseekApiKey, deepseek);
      onClose();
    } finally {
      setSaving(false);
    }
  };

  return (
    <div role="dialog" aria-label="Settings" className="settings-modal">
      <h2>Settings</h2>
      <label>
        Anthropic API Key
        <input
          type="password"
          value={anthropic}
          onChange={(e) => setAnthropic(e.target.value)}
          disabled={!loaded}
        />
      </label>
      <label>
        DeepSeek API Key
        <input
          type="password"
          value={deepseek}
          onChange={(e) => setDeepseek(e.target.value)}
          disabled={!loaded}
        />
      </label>
      <div className="settings-actions">
        <button onClick={onClose} disabled={saving}>
          Cancel
        </button>
        <button onClick={onSave} disabled={saving || !loaded}>
          Save
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 10.4: 跑测试 → PASS**

Run: `pnpm test src/settings/Settings.test.tsx`
Expected: 4 tests pass.

- [ ] **Step 10.5: 挂到 App**

Replace `app/src/App.tsx`:

```tsx
import { useState } from "react";
import "./App.css";
import { Settings } from "./settings/Settings";

function App() {
  const [settingsOpen, setSettingsOpen] = useState(false);

  return (
    <main className="container">
      <h1>Lyra</h1>
      <p>Sprint 0 skeleton — nothing here yet.</p>
      <button onClick={() => setSettingsOpen(true)}>Settings</button>
      <Settings open={settingsOpen} onClose={() => setSettingsOpen(false)} />
    </main>
  );
}

export default App;
```

Update `app/src/App.test.tsx` (still passes with just "lyra" match) — verify by running.

- [ ] **Step 10.6: 全套测试 PASS**

Run: `pnpm test`
Expected: 全部 pass。

- [ ] **Step 10.7: Commit**

```bash
cd /Users/daoyu/Documents/my-github/Lyra-music-player
git add "app/src"
git commit -m "$(cat <<'EOF'
feat(lyra): Settings modal for API key management (Sprint 0 T10)

- Settings.tsx with Anthropic + DeepSeek password inputs
- Loads existing secrets on open; save flushes via setSecret
- Mounted in App.tsx behind a button
- 4 vitest RTL cases

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: Sprint 0 Acceptance Smoke Test

**Files:** (none created here; this is verification)

**Interfaces Produced:** 无

**Interfaces Consumed:** 全部前置任务

- [ ] **Step 11.1: 全套自动测试**

Run:
```bash
cd app
pnpm test
```

Expected: 所有 vitest 用例 pass（当前累计 ~29 个：App 1 + audio 4 + db 3 + registry 5 + anthropic 4 + deepseek 4 + secrets TS 5 + Settings 4）。

Run:
```bash
cd app/src-tauri
cargo test
```

Expected: 所有 Rust 用例 pass（audio 4 + secrets 3）。

- [ ] **Step 11.2: Typecheck**

Run:
```bash
cd app
pnpm typecheck
```

Expected: 0 errors。

- [ ] **Step 11.3: Prod build 冒烟**

Run:
```bash
cd app
pnpm build
```

Expected: `dist/` 目录生成，无错误。（`pnpm tauri build` 是 macOS bundle，可选，第一次会久，本 sprint 不强求。）

- [ ] **Step 11.4: 手动 UX smoke**

Run: `pnpm dev`

按以下顺序验证：
1. 窗口显示 "Lyra" 和 "Settings" 按钮 ✓
2. 点 Settings → 弹出 modal，两个 input 都是 password 类型 ✓
3. 输入两个占位值（例如 `sk-test-a`, `sk-test-d`）→ Save → modal 关闭 ✓
4. 再点 Settings → 重新弹出 → 两个输入框自动填上之前保存的值 ✓
5. 手动 verify keyring 有值（macOS）：
   ```bash
   security find-generic-password -s "com.daoyu.lyra" -a "provider.anthropic.apiKey"
   ```
   Expected: 输出 attribute（可能不显示明文密码，这是正常的）
6. 关闭 modal，关闭窗口

7. Verify DB 已建：
   ```bash
   sqlite3 ~/Library/Application\ Support/com.daoyu.lyra/lyra.db ".tables"
   ```
   Expected: 10 张表全在。

- [ ] **Step 11.5: 清理测试用凭证**

```bash
cd app
pnpm dev
# 打开 Settings → 清空两个字段 → Save → 关闭
# 或用命令：
security delete-generic-password -s "com.daoyu.lyra" -a "provider.anthropic.apiKey" || true
security delete-generic-password -s "com.daoyu.lyra" -a "provider.deepseek.apiKey" || true
```

- [ ] **Step 11.6: 最终 verification commit**

```bash
cd /Users/daoyu/Documents/my-github
git status
```

Expected: 干净的 working tree（无 uncommitted changes）。

```bash
git log --oneline | head -12
```

Expected: 看到 T1-T10 的 10 个 `feat(lyra):` commits + 之前的 spec commit。

**Sprint 0 完成。**

---

## Self-Review

**Spec 覆盖度检查：**
- ✅ Section 1.4 技术栈决策：Tauri + React + TS，SQLite，无 Python sidecar → Tasks 1, 4 落地
- ✅ Section 1.5 数据存储 10 张表 → Task 4 完整迁移
- ✅ Section 3.5 Model Provider 抽象层 → Tasks 6-8
- ✅ Section 6.3 Sprint 0 验收（"空壳 UI 能播本地文件" + 所有基础设施 primitives） → Tasks 2-3 + 11
- ✅ Global Constraints 中的 API key 存 Tauri keychain → Task 9
- ⚠️ 音频"通过 React UI 选文件播放"这一具体交互没有在 Sprint 0 明确要求（spec 只要求"能播本地文件"）——已在 Task 2 + 3 完成 Rust/TS 双向能力，UI 挂载留到 Sprint 1（此时才引入对话 UI，可顺便挂音频控件）。这是有意的 defer，不是漏项。
- ⚠️ Section 3.5 中列出的 provider（zhipu, doubao, ollama）Sprint 0 不实现——spec §6.2 明确 v0.1 只要 Anthropic + DeepSeek，本 plan 与 spec 一致。

**Placeholder 扫描：**
- 无 "TODO / TBD / 后续补充" 出现。
- 所有 code block 都完整。
- 所有命令都可执行（`pnpm`, `cargo`, `git`, `sqlite3`, `security`, `ffmpeg`）。
- 一处 Note：ffmpeg 生成 fixture 依赖用户机器有 ffmpeg，Task 2.2 已给出 fallback 提示。

**类型/接口一致性：**
- `ChatMessage`, `ChatResponse`, `ChatOptions`, `ModelProvider` 在 Task 5 定义，Tasks 6/7/8 都严格引用。
- `SECRET_KEYS.anthropicApiKey` / `.deepseekApiKey` 在 Task 9 定义，Task 10 引用。
- Rust `crate lib name = "lyra_lib"` 在 Task 2 定义，Task 9 tests 复用（`use lyra_lib::secrets;`）。
- `DB_URL = "sqlite:lyra.db"` 在 Task 4 定义，Rust 侧 migration 键使用同一字符串 `"sqlite:lyra.db"`。

**Global Constraints 落地：**
- Node 20 / Rust 1.75 / pnpm 8：Task 1.1 明确校验
- Tauri 2：`pnpm create tauri-app@latest` 默认拉 Tauri 2
- No Python：全部 TS + Rust，无 `pip` / `python` 命令
- Secrets via keychain：Task 9 强制 keyring
- Bundle id `com.daoyu.lyra`：Task 1.2 CLI 参数明确
- Commit prefix `feat(lyra):`：所有 commit 命令都遵守

---

## Execution Handoff

**Plan complete and saved to `音乐播放器/docs/superpowers/plans/2026-07-06-v0.1-sprint-0-skeleton.md`.**

**Two execution options:**

**1. Subagent-Driven (recommended)** — 我为每个 Task 派一个 fresh subagent，Task 之间我 review，快速迭代
**2. Inline Execution** — 直接在当前 session 用 executing-plans skill 批量跑，带 checkpoint 让你 review

**Which approach?**

**If Subagent-Driven chosen:**
- **REQUIRED SUB-SKILL:** Use superpowers:subagent-driven-development
- Fresh subagent per task + two-stage review

**If Inline Execution chosen:**
- **REQUIRED SUB-SKILL:** Use superpowers:executing-plans
- Batch execution with checkpoints for review
